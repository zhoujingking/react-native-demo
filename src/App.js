import React from 'react';
import { View } from 'react-native';
import PropTypes from 'prop-types';

function App(props) {
  return (
    <View></View>
  );
}
App.defaultProps={};
App.propTypes={};
export default App;